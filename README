NDL-OWL-enabled GUI based on JUNG, GWT, Swing and Kiwi

'mvn webstart:jnlp-single' produces a zip file that contains JNLP that should be deployed at the URL specified in the POM (geni-images.renci.org/webstart).
